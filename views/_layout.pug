doctype html 
html.h-100(lang="zh-CN")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(name="author", content="DCTewi")
        link(
            href='https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/css/bootstrap.min.css' 
            rel='stylesheet'
            crossorigin='anonymous'
        )
        link(rel='icon' href='/favicon.ico')
        link(href='/css/site.css' rel='stylesheet')
        if title == siteName 
            title #{siteName}
        else 
            title #{title} - #{siteName}
    body.d-flex.flex-column.h-100
        script(src='/js/preload.js')
        main.flex-shrink-0
            div.dropdown.float-end.mt-3.me-3
                button#langSelector.btn.btn-sm.btn-outline-primary.dropdown-toggle(type='button' data-bs-toggle='dropdown' aria-expanded='false')
                    | 🌏
                ul.dropdown-menu(aria-labelledby='langSelector')
                    li
                        btn.dropdown-item(onclick='setlang("zh")') zh
                    li
                        btn.dropdown-item(onclick='setlang("en")') en
                    li
                        btn.dropdown-item(onclick='setlang("ja")') ja
            div.container
                block content
        footer.footer.mt-auto.py-3.bg-light
            div.containter
                p.text-center.text-muted 
                    btn.text-decoration-none.link-primary(data-bs-toggle='modal' data-bs-target='#henshinModal') ⚡ 
                    | Powered by © 
                    a.text-decoration-none(href='https://github.com/DCTewi/TewiMashimaro' target='_blank') TewiMashimaro
                div#henshinModal.modal.fade(tabindex='5' aria-labelledby='henshinLabel')
                    div.modal-dialog.modal-dialog-centered
                        div.modal-content
                            div.modal-header
                                h5#henshinLabel.modal-title ⚡
                                button.btn-close(type='button' data-bs-dismiss='modal' aria-label='Close')
                            div.modal-body
                                    form.needs-validation.mb-3(method='get' action='me' novalidate)
                                        .form-floating.mb-3
                                            input#floatingKey.form-control(type='password' name='key' placeholder='key' required)
                                            label(for='floatingKey') Authorise 
                                        script(src='/js/validator.js')
                                        button.w-100.btn.btn-outline-primary(type='submit') Henshin!

        script(
            src='https://cdn.jsdelivr.net/npm/bootstrap@5.1.1/dist/js/bootstrap.bundle.min.js' 
            crossorigin='anonymous'
        )
